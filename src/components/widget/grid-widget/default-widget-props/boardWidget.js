const boardWidgetDefaultProps = ()=>({
    id : Math.round(Math.random()*10000).toString(),
    title : 'NULL',
    boardId : 'TEST',
    viewLength : '10',
    pageLength : '5',
})

export default boardWidgetDefaultProps